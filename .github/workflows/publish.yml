name: Publish

on:
  push:
    branches: [main]
    tags: ['v*']

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish:
    uses: jruk8/plugin-conventions/.github/workflows/publish.yml@v1.0.1
    secrets: inherit
